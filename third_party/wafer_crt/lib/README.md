# Wafer CRT Libraries

This directory contains the repo-local `libvr.a` used by
`tools/wafer_device_link.py`.  The archive is debug-stripped for compatibility
with the vendored Xuantie GNU ld 2.35 final-link step.
