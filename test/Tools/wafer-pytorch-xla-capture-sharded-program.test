# REQUIRES: stablehlo, pytorch-xla-importer

# RUN: rm -rf %t.matrix
# RUN: bash -c 'for strategy in data column row 2d-output 2d-contracting-output partial-replication; do \
# RUN:   env CPU_NUM_DEVICES=16 PJRT_DEVICE=CPU %importer_python %wafer_src_root/test/Tools/Inputs/wafer_pytorch_xla_capture.py --emit-sharded-program --sharding-strategy ${strategy} --output-program-dir %t.matrix/${strategy} || exit 1; \
# RUN:   test -s %t.matrix/${strategy}/functions/forward.stablehlo.bc || exit 1; \
# RUN:   test ! -e %t.matrix/${strategy}/functions/forward.mlir || exit 1; \
# RUN:   test -f %t.matrix/${strategy}/data/weight || exit 1; \
# RUN:   test -f %t.matrix/${strategy}/data/bias || exit 1; \
# RUN:   %stablehlo_translate --deserialize %t.matrix/${strategy}/functions/forward.stablehlo.bc -o %t.matrix/${strategy}.mlir || exit 1; \
# RUN:   FileCheck %s --input-file=%t.matrix/${strategy}.mlir --check-prefix=MLIR || exit 1; \
# RUN:   wafer-verify-program --program-dir %t.matrix/${strategy} | FileCheck %s --check-prefix=VERIFY || exit 1; \
# RUN: done'

# MLIR: func.func @main
# MLIR: mhlo.sharding
# MLIR: stablehlo.dot_general

# VERIFY: wafer-verify-program: verified portable StableHLO program parameters=2
